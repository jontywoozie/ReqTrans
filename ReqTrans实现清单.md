1. 项目初始化  
- 建目录：`backend/`、`frontend/`、`README.md`、`.env.example`  
- 选定依赖并写 `requirements.txt`（或 `package.json`）  
- 验收：本地能启动一个空服务

2. 定义输入输出协议  
- 约定请求字段：`direction`、`text`、`stream`  
- 约定响应字段：`translated_text`、`meta`、`missing_info`  
- 验收：接口文档草稿完成

3. 提示词模板（核心）  
- 写两套 system prompt：`产品->开发`、`开发->产品`  
- 固定输出结构（要点列表 + 待确认问题）  
- 验收：同一输入多次输出结构稳定

4. 翻译服务封装  
- 封装 LLM 调用层（统一错误处理、超时、重试）  
- 支持普通返回 + 流式 token 返回  
- 验收：命令行可调用并拿到结果

5. 后端 API  
- `POST /translate`（非流式）  
- `POST /translate/stream`（SSE/流式）  
- 参数校验与错误码处理  
- 验收：Postman/curl 可正常调用

6. 前端最小界面  
- 方向选择、输入框、提交按钮、结果区  
- 流式展示（边生成边渲染）  
- 验收：页面可完整走通一次翻译

7. 加分能力（可选）  
- 场景识别：需求讨论 vs 技术方案  
- 自动补全缺失信息（输出“需补充字段”）  
- 第三角色支持（运营/管理层）  
- 验收：新增开关可控制这些能力

8. 测试与稳定性  
- 至少 2 个样例（产品视角、开发视角）  
- 异常测试：空输入、超长输入、API Key 缺失  
- 验收：关键路径无报错

9. 文档交付  
- README：安装、配置 API Key、运行方式、接口说明  
- 写清提示词设计思路与测试样例输出  
- 验收：新同学按 README 5 分钟内跑起来

